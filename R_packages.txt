# R packages required for the analyses in R_analysis/
#
# Install at the R console:
#   install.packages(c("ordinal", "brant", "MASS", "lme4", "car"))

ordinal        # cumulative-link mixed models (clmm) — principal H4/H5 model
brant          # Brant test for proportional-odds assumption
MASS           # polr() ordinal regression (Brant input)
lme4           # crossed random-effects GLMM (robustness check)
car            # variance inflation factors (vif())
